# System Health Monitor (Bash)

This project is a Bash script that monitors system resources using Linux commands.

## Features
- Monitors CPU, memory, and disk usage
- Logs output with timestamps
- Alerts when usage exceeds thresholds
- Checks service status (nginx)

## Tech Used
- Bash scripting
- Linux commands: top, free, df
